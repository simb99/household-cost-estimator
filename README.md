# Household Cost Estimator

A Python project that calculates daily and weekly household utility costs based on user input.

This repository includes:
- CLI version using input/output in the terminal
- GUI version built with Tkinter and ttk for a graphical interface

The project demonstrates functions, dictionaries, input validation, and basic application design.
